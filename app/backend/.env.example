# -------------------------------------------------
# Configuração do Banco de Dados (PostgreSQL)
# -------------------------------------------------
# URL de conexão completa que o SQLAlchemy e o Alembic usam.
# Formato: postgresql://[USER]:[PASSWORD]@[HOST]:[PORT]/[DB_NAME]
# Deixe os valores aqui ou substitua-os abaixo.
DATABASE_URL=postgresql://seu_usuario_postgres:sua_senha_postgres@localhost:5432/db_flutter

# --- Configuração alternativa (se sua aplicação ler campos separados) ---
DB_HOST=localhost
DB_PORT=5432
DB_NAME=db_flutter
DB_USER=seu_usuario_postgres
DB_PASSWORD=sua_senha_postgres

# -------------------------------------------------
# Configuração da Aplicação (FastAPI)
# -------------------------------------------------

# Chave secreta para assinar tokens JWT (JSON Web Tokens)
# Gere uma nova chave segura usando o comando no terminal:
# > openssl rand -hex 32
SECRET_KEY=sua_chave_secreta_super_segura_aqui

# Algoritmo de assinatura do JWT
ALGORITHM=HS256

# Tempo de expiração do token de acesso (em minutos)
ACCESS_TOKEN_EXPIRE_MINUTES=30
